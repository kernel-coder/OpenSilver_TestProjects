<UserControl x:Class="Virtuoso.Core.Controls.PatientPhotoUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             x:Name="patient_photo_ctrl"
             mc:Ignorable="d"
             d:DesignHeight="300"
             d:DesignWidth="400">

    <Grid x:Name="LayoutRoot">
        <Grid x:Name="ControlRoot"
              DataContext="{Binding ElementName=patient_photo_ctrl}">
            <Image Source="{Binding DataContext.DefaultPhoto, ElementName=ControlRoot, Mode=TwoWay}"
                   Visibility="{Binding DataContext.HavePhotos, ElementName=ControlRoot, Converter={StaticResource OppositeVisibilityConverter}}"
                   MaxHeight="150" />
            <Image Source="{Binding Photo, Converter={StaticResource PhotoConverter}}"
                   MaxHeight="150" />
        </Grid>
    </Grid>

</UserControl>