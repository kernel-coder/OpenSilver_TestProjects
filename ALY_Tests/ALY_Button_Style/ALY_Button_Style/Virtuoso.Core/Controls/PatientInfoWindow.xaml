<controls:ChildWindow x:Class="Virtuoso.Core.Controls.PatientInfoWindow"
                      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                      xmlns:controls="clr-namespace:System.Windows.Controls;assembly=System.Windows.Controls"
                      xmlns:Core="clr-namespace:Virtuoso.Core;assembly=Virtuoso.Core"
                      Title="PatientInfoWindow"
                      Style="{StaticResource ChildWindowStyle}"
                      Core:BindableDialogResult.DialogResult="{Binding DialogResult, Mode=TwoWay}"
                      Width="900"
                      Height="550">
    <Grid x:Name="LayoutRoot" Margin="2">
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!--<Button x:Name="CancelButton" Content="Cancel" Click="CancelButton_Click" Width="75" Height="23" HorizontalAlignment="Right" Margin="0,12,0,0" Grid.Row="1" />
        <Button x:Name="OKButton" Content="OK" Click="OKButton_Click" Width="75" Height="23" HorizontalAlignment="Right" Margin="0,12,79,0" Grid.Row="1" />-->
    </Grid>
</controls:ChildWindow>